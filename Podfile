# Uncomment the next line to define a global platform for your project
# プロジェクトのグローバル プラットフォームを定義する場合、次の行のコメントを外す
platform :ios, '14.4'

target 'Nasa' do
  # Comment the next line if you don't want to use dynamic frameworks
  # Dynamic Framework を使用しない場合、次の行をコメント
  use_frameworks!

  # Pods for Nasa
  # Nasa アプリの Pod
  target 'NasaTests' do
    inherit! :search_paths
    # Pods for testing
    # テスト用の Pod
  end

  target 'NasaUITests' do
    # Pods for testing
    # テスト用の Pod
  end

end
